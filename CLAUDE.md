# Agentic

New repository — no application code yet. This file is the working agreement for
Claude Code sessions in this repo; fill in the sections below as the project takes shape.

## Scope

Unrelated to any other repo on this machine. Nothing from another project — code,
config, data, or notes — should be copied in without a deliberate decision to do so.

## Stack

_TBD._

## Commands

_TBD — add build / test / lint invocations here once they exist._

## Conventions

- Commit after each self-contained unit of work.
- No secrets in the repo; keep real values in `.env` (gitignored) and document the
  keys in `.env.example`.
